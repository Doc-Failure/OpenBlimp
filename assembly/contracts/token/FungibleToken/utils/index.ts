@nearBindgen
export class tMetadata{
    name: string;
    symbol: string;
    decimals: u8;
    spec: string;
    icon: string;
    reference: string;
    reference_hash: string;
}

export type FungibleTokenMetadata = tMetadata;